# TIndicator  (guide pp. 458–460)

Rust module(s): `src/widgets/indicator.rs`   |   magiblot: `include/tvision/editors.h` / `source/tvision/tindictr.cpp`

> TIndicator has **two own fields** documented by the guide (`Location`,
> `Modified`), five documented entries (Init, Draw, GetPalette, SetState,
> SetValue), and one palette (`CIndicator`, 2 entries).

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `Location` (field) | 458 | PORTED | OK | `tv::Indicator::location: Point` | 2 | Guide: `TPoint` holding current column and line position. Rust `Point` = `i32` coords, same shape. Field is `pub`. Doc explains what it stores and that it is zero-based (displayed 1-based), but stops short of explaining the broker path by which the editor updates it (via `Deferred::IndicatorSetValue`). |
| `Modified` (field) | 458–459 | PORTED | OK | `tv::Indicator::modified: bool` | 2 | Guide: `Boolean`; True if buffer has unsaved changes. Rust `bool`, identical semantics. Pub field. Doc explains the flag and the `☼` marker it triggers, but doesn't mention the deferred update path. |
| `Init` (constructor) | 459 | PORTED | OK | `tv::Indicator::new(bounds: Rect) -> Indicator` | 2 | Guide: calls `TView.Init`, sets `GrowMode = gfGrowLoY + gfGrowHiY`. C++: `growMode = gfGrowLoY \| gfGrowHiY`. Rust `new` sets `grow_mode.lo_y = true; grow_mode.hi_y = true`. Matches. Guide also notes the indicator is not selectable (display-only); Rust does not set `ofSelectable` (correct by omission). "How/when to construct" + the deferred update wiring could be mentioned in rustdoc. |
| `Draw` (method) | 459 | PORTED | OK | `tv::Indicator::draw` (impl `View::draw`) | 3 | Guide: draws `line:column` form, shows `☼` if Modified. C++: fills with `dragFrame`/`normalFrame` chars, `b.putChar(0,15)` for modified, `moveStr(8-colon_offset, s, color)`. Rust replicates all three steps faithfully. The counterintuitive C++ naming (`dragFrame` = `═` used when NOT dragging; `normalFrame` = `─` used while dragging) is explicitly called out in a comment. Colon-alignment at column 8 and the negative-start-col edge case are both documented and tested. Doc score 3: what + how + heritage section present. |
| `GetPalette` (method) | 459 | EQUIVALENT | OK | `tv::Role::IndicatorNormal` + `tv::Role::IndicatorDragging` via `tv::Theme` | 2 | C++: returns `cpIndicator = "\x02\x03"` — 2 entries mapping to app-palette entries 2 (normal) and 3 (dragging/active). Rust uses `Role::IndicatorNormal` → palette index 26, `Role::IndicatorDragging` → index 27; the theme chain comment in `src/theme.rs` line 831–832 documents the full derivation (`cpIndicator[1]=0x02 → cpBlueWindow[2]=0x09 → cpAppColor[9]`). Known idiomatic mapping: class Palette → `tv::Theme` (D7). Module doc mentions the two roles but doesn't explain the palette chain; a `# Turbo Vision heritage` note on `IndicatorNormal`/`IndicatorDragging` would help. |
| `SetState` (method) | 459 | EQUIVALENT | OK | `View::set_state` default (no `Indicator` override) + whole-tree redraw (D9) | 2 | Guide: calls `TView.SetState`, then calls `drawView` if `sfDragging` changed. C++ redraws immediately on dragging state change. Rust `Indicator` does NOT override `set_state`. Instead, `sfDragging` propagates via `Group::set_state` and the whole-tree redraw on every pump tick picks up the changed `state.state.dragging` field automatically (deviation D9, documented in module-level comment "The dragging state is read live from the view state each frame"). Functionally equivalent; the deliberate deviation is commented. Rustdoc on the impl block could carry an explicit note directing readers who look for the C++ `setState` override. |
| `SetValue` (method) | 459 | EQUIVALENT | OK | `tv::Indicator::set_value` + `tv::view::Deferred::IndicatorSetValue` broker | 2 | Guide: sets `Location` and `Modified`, then calls `drawView`. C++: also has a no-op guard — skips the update if neither value changed. Rust `set_value` always writes (no guard), relying on the whole-tree redraw being unconditional (D9). The method itself is called only via the `Deferred::IndicatorSetValue` broker (editor is a leaf, cannot mutate its sibling inline). The rustdoc says "no broadcast" and "whole-tree redraw reads new values automatically" but does not mention the no-op guard omission or the deferred broker path — useful for callers. |
| `CIndicator` palette (2 entries) | 459 | EQUIVALENT | OK | `tv::Role::IndicatorNormal`, `tv::Role::IndicatorDragging` | 2 | Guide: `CIndicator` maps entries 1–2 to app-palette slots 2 (frame passive = Normal) and 3 (frame active = Dragging). Rust: two named `Role` variants with the full derivation chain documented in `src/theme.rs`. Known idiomatic mapping: class Palette → `tv::Theme`. The `Role` enum items score 2 (what they are, not how they compose with the palette chain). |
| `TStreamable` / stream support | 459 | NOT-PORTED | — | — | N/A | `TIndicator::build()` + stream constructor. Dropped per project-wide decision: `TStreamable` is not ported (serde-if-revived). |

## Summary

- PORTED: 4   EQUIVALENT: 3   NOT-PORTED: 1   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 6   |   → concept: 0
- Notable finding: No missing or suspect items. The most important gap is that `SetValue`/`set_value` silently drops the C++ no-op guard (skip update when location and modified are unchanged) because the whole-tree redraw makes it harmless — but this deviation is uncommented in the rustdoc, and the deferred-broker update path (`Deferred::IndicatorSetValue`) is not mentioned, leaving callers without a route back to the liveness invariant.
