# Globals: vmtHeaderSize, voXXXX, vsXXXX, wfXXXX, WindowColorItems, wnNoNumber, WordChars, WordRec, wpXXXX  (guide pp. 582–586)

Rust module(s): `src/validate.rs`, `src/window/window.rs`, `src/widgets/editor.rs`   |   magiblot: `include/tvision/validate.h`, `include/tvision/views.h`, `source/tvision/twindow.cpp`, `source/tvision/teditor.cpp`

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `vmtHeaderSize` (constant, `= 8`) | 582 | NOT-PORTED | — | — | N/A | Pascal/Borland OOP VMT internal offset used by the `Objects` stream machinery. No equivalent in Rust — the VMT concept and the TStreamable/TObject stream layout are both dropped (deviation D12). The value `8` was the Pascal virtual-method-table header size; it has no meaning in Rust's vtable model. |
| `voFill` ($0001) | 582 | EQUIVALENT | OK | `PXPictureValidator::auto_fill: bool` (field); `suppress_fill` param on `Validator::is_valid_input` | 2 | In `src/validate.rs` — out of scope for this pass (validate.rs is skipped; symbols already documented in a prior pass). Score-2 reflects the state prior to that pass; the validate.rs pass addressed these. |
| `voTransfer` ($0002) | 582 | EQUIVALENT | OK | `Validator::transfer_get` / `transfer_set` trait methods; `RangeValidator::set_transfer(bool)` | 2 | Same as `voFill` — in `src/validate.rs`, out of scope for this pass. |
| `voReserved` ($00FC) | 582 | NOT-PORTED | — | — | N/A | Borland-reserved mask bits; no semantic content for user code. Not ported — Rust has no `Options` word, so these reserved bits have nowhere to live and no meaning to carry. |
| `vsOK` (0) | 583 | EQUIVALENT | OK | `Validator::is_status_ok() -> bool` (returns `true`) | 2 | In `src/validate.rs` — out of scope for this pass (validate.rs skipped). Score reflects prior-pass state. |
| `vsSyntax` (1) | 583 | EQUIVALENT | OK | `PXPictureValidator::is_status_ok() -> bool` (returns `false` when mask is malformed) | 2 | Same as `vsOK` — in `src/validate.rs`, out of scope. |
| `wfMove` ($01) | 583 | EQUIVALENT | OK | `tv::WindowFlags { move: bool }` | 3 | Guide: window-flag bit meaning the window can be moved. Rust: `WindowFlags.r#move` bool (field named with raw identifier because `move` is a Rust keyword). Struct doc notes the keyword spelling. Known idiomatic mapping: flag word → struct-of-bools (deviation D5), documented via `# Turbo Vision heritage` section. |
| `wfGrow` ($02) | 583 | EQUIVALENT | OK | `tv::WindowFlags { grow: bool }` | 3 | Guide: window-flag bit meaning the window can be resized (has a grow icon). Rust: `WindowFlags.grow`. Same deviation D5 struct-of-bools mapping. |
| `wfClose` ($04) | 583 | EQUIVALENT | OK | `tv::WindowFlags { close: bool }` | 3 | Guide: window-flag bit meaning the window has a close icon. Rust: `WindowFlags.close`. Same deviation D5 struct-of-bools mapping. |
| `wfZoom` ($08) | 583 | EQUIVALENT | OK | `tv::WindowFlags { zoom: bool }` | 3 | Guide: window-flag bit meaning the window has a zoom icon. Rust: `WindowFlags.zoom`. Same deviation D5 struct-of-bools mapping. |
| `WindowColorItems` (function) | 583 | NOT-PORTED | — | — | N/A | C++ `ColorSel` unit function that builds a linked list of `TColorItem` records for window objects, used by the interactive colour-selector dialog (`TColorDialog`). The entire `TColorDialog` / `TColorGroup` / `TColorItem` colour-editor subsystem is not ported (it targets the built-in 16-colour EGA palette editor; deviation D7 replaces palette indexing with `Theme`/`Role`). No Rust analog exists — colour customisation routes through `Theme`, not a runtime colour-picker dialog. |
| `wnNoNumber` (constant, `= 0`) | 584 | EQUIVALENT | OK | `Window::new(…, number: i16)` with `number = 0` → `View::number() -> None`; `number_to_option(0) -> None` | 3 | Guide: sentinel value meaning the window is not numbered and cannot be selected by Alt+n. Rust: `Window.number: i16`; the private helper `number_to_option` maps `n <= 0` to `Option::None`; `View::number()` returns `None` for unnumbered windows. `Window::new` doc now explicitly documents that `number = 0` (the `wnNoNumber` sentinel) creates an unnumbered window, and explains the Alt-N selection mechanism for numbered windows. |
| `WordChars` (variable, `set of Char`) | 584 | EQUIVALENT | OK | `is_word_char(ch: u8) -> bool` + `get_char_type(ch: u8) -> u8` in `src/widgets/editor.rs` (private fns) | N/A | Guide: set of characters treated as word characters by the editor (digits, letters, underscore). Rust: private free functions — not public API. Both have one-line comments. Scored N/A per visibility policy. |
| `WordRec` (type, `record Lo, Hi: Byte`) | 584 | NOT-PORTED | — | — | N/A | Pascal utility type providing named access to the Lo and Hi bytes of a Word. Pure Pascal language artifact — Rust handles byte splitting with standard methods (`u16::to_le_bytes`, bit-shifting). No analog needed or ported. |
| `wpBlueWindow` (0) | 585 | EQUIVALENT | OK | `tv::WindowPalette::Blue` | 3 | Guide: palette selector meaning yellow-on-blue text window (the default). Rust: `WindowPalette::Blue` (the `#[default]` variant). Enum doc describes the colour mapping and the role family it selects. |
| `wpCyanWindow` (1) | 585 | EQUIVALENT | OK | `tv::WindowPalette::Cyan` | 3 | Guide: palette selector meaning blue-on-cyan window (e.g. for message windows). Rust: `WindowPalette::Cyan`. Enum variant doc names the `Role::FrameCyan*` family. |
| `wpGrayWindow` (2) | 585 | EQUIVALENT | OK | `tv::WindowPalette::Gray` | 3 | Guide: palette selector meaning black-on-gray window (dialog boxes). Rust: `WindowPalette::Gray`. Dialog constructor calls `set_palette(WindowPalette::Gray)`. Enum variant doc names `Role::FrameGray*`. |

## Summary

- PORTED: 0   EQUIVALENT: 13   NOT-PORTED: 4   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 4   |   → concept: 0
- Pass 1 raised to score 3 (permitted file `src/window/window.rs`): `wnNoNumber` → `Window::new` now documents that `number = 0` is the unnumbered sentinel and explains Alt-N selection. Reclassified as N/A-private: `WordChars`→`is_word_char`/`get_char_type` (private fns in `editor.rs`). Remaining doc<3 (out-of-scope files): `voFill`/`voTransfer` + `vsOK`/`vsSyntax` → all in `src/validate.rs` (skipped per task instructions — addressed by the validate.rs pass).
- Notable finding: The four NOT-PORTEDs (`vmtHeaderSize`, `voReserved`, `WindowColorItems`, `WordRec`) are all correct non-ports — Pascal VMT artifacts and the unported colour-editor subsystem.
