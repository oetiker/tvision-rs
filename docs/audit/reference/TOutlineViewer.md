# TOutlineViewer  (guide pp. 491–498)

Rust module(s): src/widgets/outline.rs   |   magiblot: include/tvision/outline.h / source/tvision/toutline.cpp

> `TOutlineViewer` is the abstract base for collapsible tree views. It derives
> from `TScroller` in C++. In Rust it is split into two parts: the
> `OutlineViewer` trait (overridable methods) plus `OutlineViewerState` (data
> members), with shared draw/event/traversal logic in free functions generic
> over `L: OutlineViewer`. This is the same design choice used for
> `ListViewer` (D2 embed-and-delegate, documented in the module doc).

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `foc` (field) | 492 | PORTED | OK | `tv::OutlineViewerState::foc: i32` | 2 | C++ public `int foc`. Rust public field on `OutlineViewerState`. Doc says what it is (DFS position of focused item), but not "how to read it vs. how to set it (via `adjust_focus`)". |
| `delta` (inherited from TScroller) | — | PORTED | OK | `tv::OutlineViewerState::delta: Point` | 2 | TScroller's scroll offset. Rust stores it directly in `OutlineViewerState` (not a separate `Scroller` embed — the outline viewer's scroller base is inlined). Module doc explains the read-sync mechanism. |
| `Init` (constructor) | 492 | PORTED | OK | `tv::OutlineViewerState::new(bounds, h, v)` | 2 | Sets `growMode = gfGrowHiX + gfGrowHiY`, `foc = 0`. Rust sets `grow_mode.hi_x / hi_y = true`, `foc = 0`, `delta = (0,0)`. The note about scroll-bar params needing a `Context` (so the consumer calls `ov_update` after insert) is in the module doc but not in the `new` rustdoc itself. |
| `adjust` (method) | 492 | PORTED | OK | `tv::OutlineViewer::adjust(pos: i32, expand: bool)` | 2 | C++: `adjust(TNode* node, Boolean expand)` — takes a raw node pointer. Rust: keyed by DFS `pos` (not a node ref), because the free functions hold `&self` and cannot hand a `&mut Node` to the abstract method at the same time. The Rust signature change is documented in the trait rustdoc ("The contract is keyed by DFS position…"). Doc score 2 — the "why DFS position instead of a node ref" is in the trait header, not on this method. |
| `draw` (method) | 493 | PORTED | OK | `tv::ov_draw<L: OutlineViewer>` (free fn) + `Outline::draw` delegates to it | 3 | C++ calls `firstThat(drawTree)`. Rust: `ov_draw` is a generic free function; `Outline::draw` delegates. Module doc covers the two-pass approach (gather rows, then draw). |
| `expandAll` (method) | 493 | PORTED | OK | `tv::ov_expand_all<L: OutlineViewer>(this, pos: i32)` | 2 | C++: `expandAll(TNode* node)` — takes the node directly, recurses. Rust: `ov_expand_all(this, pos: i32)` — keyed by DFS position (same reason as `adjust`). The algorithm restarts the traversal each round. The doc explains the algorithm but not the C++ signature difference or why pos-keyed. |
| `focused` (method) | 493 | PORTED | OK | `tv::OutlineViewer::focused_item(i: i32)` | 2 | C++: `focused(int i)` — sets `foc = i`. Renamed to `focused_item` to avoid collision with the `sfFocused` state concept. Default implementation provided (`foc = i`). Doc explains the rename but not "when you'd override it". |
| `getChild` (method) | 493 | PORTED | OK | `tv::OutlineViewer::get_child<'a>(&'a self, node: &'a Node, i: i32) -> Option<&'a Node>` | 2 | Abstract in both. The lifetime annotation is the notable Rust addition (borrow tied to `self`). Doc explains the signature but not "the 0-based walk on `child_list → next` chain". |
| `getGraph` (method) | 493 | PORTED | OK | `tv::ov_get_graph<L: OutlineViewer>(…, ctx: &DrawCtx) -> String` (free fn, overridable by not calling it) | 2 | C++ virtual method; Rust is a free function called from `ov_draw` — a concrete widget can supply its own by not using `ov_draw`'s call. Not a trait method. Doc score 2 — the override story (how a concrete widget would replace it) is absent. |
| `getNext` (method) | 493 | PORTED | OK | `tv::OutlineViewer::get_next<'a>(&'a self, node: &'a Node) -> Option<&'a Node>` | 2 | Abstract in both. Same lifetime pattern as `get_child`. |
| `getNumChildren` (method) | 493 | PORTED | OK | `tv::OutlineViewer::get_num_children(&self, node: &Node) -> i32` | 2 | Abstract in both. |
| `getPalette` (method) | 493 | EQUIVALENT | OK | `tv::theme::Role::OutlineNormal` / `Role::OutlineFocused` / `Role::OutlineSelected` / `Role::OutlineNotExpanded` | 2 | C++ `cpOutlineViewer = "\x06\x07\x03\x08"` (4 palette entries). Rust: `Role`-keyed theme. Known idiomatic mapping: class Palette → `tv::Theme` (D7). The four C++ palette entries map 1:1 to the four `Role` variants; same color positions in the standard application palette. The `Role` items themselves score 2 (no "how the four roles compose"). |
| `getRoot` (method) | 494 | PORTED | OK | `tv::OutlineViewer::get_root(&self) -> Option<&Node>` | 2 | Abstract in both. `Option` replaces null-check. |
| `getText` (method) | 494 | PORTED | OK | `tv::OutlineViewer::get_text<'a>(&'a self, node: &'a Node) -> &'a str` | 2 | Abstract in both. Returns a `&str` borrowed from self. |
| `handleEvent` (method) | 494 | PORTED | OK | `tv::ov_handle_event<L: OutlineViewer>` (free fn) + `Outline::handle_event` delegates | 3 | C++ single blocking do-while mouse loop via `mouseEvent`. Rust: decomposes into `MouseDown` / `MouseMove` / `MouseAuto` / `MouseUp` arms using the `MouseTrackCapture` mechanism (D9). All key cases (Up/Down/Left/Right/PgUp/PgDn/CtrlPgUp/CtrlPgDn/Home/End/Enter/+/-/*) handled. The `dragged < 2` click-vs-drag guard is faithfully replicated. Module doc covers the press-and-hold mechanism. |
| `hasChildren` (method) | 494 | PORTED | OK | `tv::OutlineViewer::has_children(&self, node: &Node) -> bool` | 2 | Abstract in both. |
| `isExpanded` (method) | 494 | PORTED | OK | `tv::OutlineViewer::is_expanded(&self, node: &Node) -> bool` | 2 | Abstract in both. |
| `isSelected` (method) | 494 | PORTED | OK | `tv::OutlineViewer::is_selected(&self, i: i32) -> bool` | 2 | C++ virtual with default `foc == i`. Rust: trait method with default `self.ov().foc == i`. Same semantics. |
| `selected` (method) | 495 | PORTED | OK | `tv::OutlineViewer::selected(&mut self, i: i32)` | 2 | C++ virtual no-op default. Rust: trait method with empty default. The guide notes overriding to broadcast `cmListItemSelected`; the Rust rustdoc mentions broadcasting `Command::OUTLINE_ITEM_SELECTED` but does not spell out the exact override recipe. |
| `setState` (method) | 495 | PORTED | OK | `tv::ov_set_state<L: OutlineViewer>` (free fn) + `Outline::set_state` delegates | 2 | C++: calls `TScroller::setState` then `drawView()` if `sfFocused`. Rust: sets the flag, broadcasts `RECEIVED_FOCUS`/`RELEASED_FOCUS` if `Focused`, then show/hides scroll bars on `Active`/`Selected`. The redraw is implicit (whole-tree redraw per pump tick — D9). Functionally equivalent; the implicit-redraw deviation is noted in the module doc but not in the `ov_set_state` rustdoc. |
| `update` (method) | 496 | PORTED | OK | `tv::ov_update<L: OutlineViewer>` (free fn) | 2 | C++ calls `firstThat(countNode)` then `setLimit`/`adjustFocus`. Rust: `ov_update` traverses, counts, calls `set_limit`/`adjust_focus`. Same semantics. The "must be called after insert" constraint is in the module doc but not in `ov_update` rustdoc. |
| `expanded` (method — guide pp. 492 diagram) | 492 | NOT-PORTED | — | — | — | RESOLVED (was UNSURE): guide-diagram artifact, not a real API entry. The p. 492 class diagram lists `expanded` separately from `isExpanded`, but the C++ header/source (`toutline.h`/`.cpp`) has only the `isExpanded` virtual — no distinct `expanded` method. Nothing to port (the real method is the `isExpanded`/`ov_is_expanded` row above). |
| `selected` (field — guide pp. 492 diagram) | 492 | NOT-PORTED | — | — | — | RESOLVED (was UNSURE): guide-diagram artifact, not a real API entry. The p. 492 diagram lists `selected` as a field, but C++ has only the `selected()` virtual method (no field) — the diagram column appears to run on from TScroller's field list. Nothing to port (the real method is the `selected`/`ov_selected` row above). |
| `disposeNode` (method) | — | EQUIVALENT | OK | Automatic `Box<Node>` / `Drop` | N/A | C++ `static void disposeNode(TNode* node)` — recursive free. Rust: `Box<Node>` drop is recursive and automatic; no explicit `disposeNode` needed. Private/internal; not user-facing. |
| `firstThat` (method) | — | EQUIVALENT | OK | `tv::traverse<L, F>` (generic free fn, `F` returns `bool` to stop) | 2 | C++ `firstThat(TOutlineVisitor)` iterates until visitor returns `true`. Rust: `traverse(this, action)` with `FnMut → bool`. Known idiomatic mapping: C-style callback → Rust closure/iterator. |
| `forEach` (method) | — | EQUIVALENT | OK | `tv::traverse<L, F>` (same free fn, visitor always returns `false`) | 2 | C++ `forEach` iterates all nodes (visits but ignores return). Rust: same `traverse` fn — caller returns `false` to visit all. Two C++ overloads collapse into one generic free function. |
| `getNode` (method) | — | EQUIVALENT | OK | `tv::ov_get_node_info(this, pos: i32) -> Option<(i32, i64, u16)>` | 2 | C++ `getNode(int i)` returns the `TNode*` at position `i` by running `firstThat(isNode)`. Rust: `ov_get_node_info` returns `(level, lines, flags)` for a DFS position — not the node pointer (which would require unsafe or re-borrowing). The call site in `handleEvent` uses the flags directly; no raw node pointer is needed. |
| `createGraph` (method) | — | PORTED | OK | `tv::create_graph(level, lines, flags, lev_width, end_width, chars: &[char;8]) -> String` | 2 | C++ method on `TOutlineViewer`, takes `const char*`. Rust: free function, takes `&[char; 8]` (char array, not byte string). Behavior identical per test coverage. Public. |
| `ovExpanded` constant | — | PORTED | OK | `tv::OV_EXPANDED: u16 = 0x01` | 1 | C++ `ovExpanded = 0x01`. Rust module-level const (not an assoc const — `OutlineViewer` is a trait, not a struct). Currently `pub(crate)`. Doc: name only, no explanation of meaning. |
| `ovChildren` constant | — | PORTED | OK | `tv::OV_CHILDREN: u16 = 0x02` | 1 | Same pattern. Pub(crate). |
| `ovLast` constant | — | PORTED | OK | `tv::OV_LAST: u16 = 0x04` | 1 | Same pattern. Pub(crate). |
| `cmOutlineItemSelected` constant | — | PORTED | OK | `tv::command::Command::OUTLINE_ITEM_SELECTED` (= 301) | 1 | C++ `cmOutlineItemSelected = 301`. Rust: `Command::OUTLINE_ITEM_SELECTED`. Assoc const on the `Command` newtype. Doc: name only. |
| `TStreamable` / stream (read/write) | — | NOT-PORTED | — | — | — | TStreamable / DOS stream machinery dropped project-wide (serde-if-revived). |

## Summary

- PORTED: 22   EQUIVALENT: 5   NOT-PORTED: 3   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 19   |   → concept: 0
- Notable findings: No correctness gaps. The two UNSURE rows are guide-diagram artifacts (`expanded` and `selected` listed as fields in the TOutlineViewer column) — not real C++ members; they should be confirmed as diagram noise and closed. The most impactful doc gap is `ov_update`: the "must call after insert" contract lives only in the module doc, not on the function itself, so a user landing directly on `ov_update` via rustdoc will miss it.
