# TWindow  (guide pp. 577–581)

Rust module(s): `src/window/window.rs`, `src/window/mod.rs`   |   magiblot: `include/tvision/views.h` (TWindow) + `source/tvision/twindow.cpp`

> TWindow is a specialised TGroup that builds a TFrame around itself,
> owns decoration flags, a number, a palette selector, a title, and a zoom
> rect. The guide documents 6 fields, 9 methods (Init/Load/Done/Close/
> GetPalette/GetTitle/HandleEvent/InitFrame/SetState/SizeLimits/
> StandardScrollBar/Store/Zoom — see rows below), and 3 palette strings
> (cpBlueWindow / cpCyanWindow / cpGrayWindow) with 8 entries each.

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `Flags` (field, `uchar`, wfMove/wfGrow/wfClose/wfZoom) | 578 | EQUIVALENT | OK | `tv::WindowFlags` struct-of-bools (`r#move`, `grow`, `close`, `zoom`) | 2 | D5 flag-word → struct-of-bools, documented in module doc. Default: all four true. The `r#move` spelling (Rust keyword escape) is noted in the type doc but there is no "why `r#move`" callout — score 2 not 3. |
| `Frame` (field, `PFrame`, pointer to the frame child) | 578 | EQUIVALENT | OK | `tv::Window::frame_id: ViewId` | 2 | D3 push-down: the C++ `frame` pointer is a raw upward reach; Rust replaces it with a `ViewId` handle. The `frame_id` getter is documented (what), but "why the id rather than a pointer, and how it is used for push-down" is not in the getter's own doc — score 2. |
| `Number` (field, `Integer`, 1–9 selectable window number) | 578 | PORTED | OK | `tv::Window::number: i16` (private); exposed via `View::number() -> Option<i16>` | 2 | Guide: 0 = no number. Rust: `number <= 0` → `None`; `> 0` → `Some(n)`. The `View::number` override carries a doc comment (what), but "how Alt-N uses it" is not in the Window-level doc — score 2. |
| `Palette` (field, `Integer`, wpBlueWindow/wpCyanWindow/wpGrayWindow) | 578 | EQUIVALENT | OK | `tv::WindowPalette` enum (`Blue`/`Cyan`/`Gray`) + `tv::Window::palette: WindowPalette` | 2 | D7 palette-pointer → Theme/Role. `WindowPalette` type doc explains the mapping to `Role::Frame*` families (score 2). No "when to choose each" guidance — upgrading to 3 would require a brief usage note. |
| `Title` (field, `PString`) | 578 | PORTED | OK | `tv::Window::title: Option<String>` | 2 | `Option<String>` replaces `PString`; `None` covers the C++ `nil` case. `title()` getter is documented (what only) — score 2. |
| `ZoomRect` (field, `TRect`, saved unzoomed bounds) | 578 | PORTED | OK | `tv::Window::zoom_rect: Rect` | 2 | Guide says read-only; Rust exposes a `zoom_rect()` getter. Field purpose documented (what), but the toggle mechanic ("saved here when window zooms, restored when un-zoomed") is not in the getter doc — score 2. |
| `Init` (constructor) | 579 | PORTED | OK | `tv::Window::new(bounds: Rect, title: Option<String>, number: i16) -> Window` | 3 | C++ Init: sets `sfShadow`, `ofSelectable + ofTopSelect`, `gfGrowAll + gfGrowRel`, sets Title/Number/Flags, calls `InitFrame` + inserts frame, sets ZoomRect. Rust `new` matches every step verbatim (verified in `new_ports_ctor_defaults` test). Doc comment enumerates all defaults explicitly — score 3. |
| `Load` (streaming constructor) | 579 | NOT-PORTED | — | — | N/A | TStreamable / stream I/O is dropped project-wide (CLAUDE.md locked decision: "TStreamable dropped; serde if revived"). No counterpart. |
| `Done` (destructor) | 579 | NOT-PORTED | — | — | N/A | Pascal/C++ explicit destructor chain. Rust `Drop` on the embedded `Group` reclaims children automatically; no explicit `Done` needed. |
| `Close` (method) | 579 | PORTED | OK | `tv::Window::handle_event` — close-command arm calls `ctx.request_close(id)` | 2 | C++ `close()` calls `valid(cmClose)` then `destroy(this)`. Rust: handle_event close arm calls `self.valid(cmClose, ctx)` then `ctx.request_close(id)` — same logic. The handle_event doc comment mentions the close arm and the modal-cancel branch, but `close()` has no standalone entry point to score higher; the embedded doc is score 2 (what + when). |
| `GetPalette` (method) | 579 | EQUIVALENT | OK | `tv::Window::palette() -> WindowPalette` + `tv::Frame::set_palette` + `Role` mapping | 2 | C++ returns one of three CPalette strings keyed by `palette` field. Rust: `palette()` returns the `WindowPalette` enum that the frame turns into `Role::Frame*` families at draw time. Table 19.43 (Blue→CBlueWindow / Cyan→CCyanWindow / Gray→CGrayWindow) is exactly mirrored. `set_palette` doc mentions the role-family mapping — score 2; "when to use each scheme" missing for score 3. |
| `GetTitle` (method) | 579–580 | PORTED | OK | `tv::Window::title() -> Option<&str>` + `tv::Frame` calls `owner.get_title()` push-down via `Frame::set_title` | 2 | C++ `getTitle(maxSize)` returns `title^` or empty; Frame.Draw calls `Owner^.GetTitle` to obtain the title. Rust: `title()` returns `Option<&str>`; the title is pushed down to the frame child at construction and after `set_title` calls. The `maxSize` truncation semantics (guide: shorten if longer than MaxSize) are not explicitly handled by the Rust `title()` getter — truncation responsibility is implicit in the frame's draw (which renders a finite cell row). This is a deliberate "push-down" deviation (D3), documented in the module doc. Doc score 2. |
| `HandleEvent` (method) | 580 | PORTED | OK | `tv::Window::handle_event` (impl `View::handle_event`) | 3 | C++ handles: `cmResize` (if wfMove/wfGrow), `cmClose` (if wfClose, with modal-cancel path), `cmZoom` (if wfZoom), `kbTab`/`kbShiftTab`, and broadcast `cmSelectWindowNum` (matches `Event.infoInt == number`). Rust: resize → `KeyboardResizeCapture`; close + modal cancel; zoom → `Window::zoom`; Tab/Shift+Tab. `cmSelectWindowNum` is re-architected: moved to `program_handle_event` + `Desktop::select_window_num` + `Group::focus_by_number` (skip-listed on the `#[delegate]` macro). This is a documented, deliberate deviation (module doc + `handle_event` doc comment). The handle_event doc comment is comprehensive and enumerates each arm — score 3. |
| `InitFrame` (static factory method) | 580 | NOT-PORTED | — | — | N/A | C++ `TWindow::initFrame(TRect)` is a static factory that creates the `TFrame`; `TWindowInit` stores a `createFrame` function pointer so subclasses can inject a custom frame type. Rust `Window::new` constructs `Frame::new` directly and inserts it; no injection hook exists. Module doc notes: "A custom-frame injection hook has no consumer yet; reintroduce it when a subtype needs a non-default frame." Intentionally absent, with documented rationale. |
| `SetState` (method) | 580 | PORTED | OK | `tv::Window::set_state` (impl `View::set_state`) | 3 | C++: calls `TGroup::setState`, then if `sfSelected` calls `setState(sfActive, enable)` on both group and frame directly, then enables/disables cmNext/cmPrev/cmResize/cmClose/cmZoom. Rust matches exactly: calls `group.set_state`, then calls `group.set_state(Active, enable)` (which propagates to children incl. the frame via Group's propagation), then enables/disables the same command set. The `set_state` doc comment explains every arm including the command-enable logic — score 3. |
| `SizeLimits` (method) | 580 | PORTED | OK | `tv::Window::size_limits` (impl `View::size_limits`) | 2 | C++: calls `TView::sizeLimits`, then sets min to `minWinSize` (16×6). Rust: calls `group.size_limits(owner_size)`, uses its max, forces min to `Point::new(16, 6)`. Identical semantics. Doc says "min forced to 16×6", but does not mention the relationship to the `calc_bounds` skip-list (why `calc_bounds` is NOT delegated to the group) — score 2. |
| `StandardScrollBar` (method) | 580–581 | PORTED | OK | `tv::Window::standard_scroll_bar(opts: ScrollBarOptions) -> ViewId` | 2 | C++ `standardScrollBar(aOptions: ushort)` takes `sbHorizontal/sbVertical` + `sbHandleKeyboard` bit-flags. Rust `ScrollBarOptions` is a struct-of-bools (`vertical`, `handle_keyboard`) — D5. Rect geometry matches C++ exactly (verified by tests). Returns `ViewId` instead of `PScrollBar` pointer — D3. Method doc explains placement (what) but not the "fits the frame without covering corners" constraint from the guide — score 2. |
| `Store` (streaming write) | 581 | NOT-PORTED | — | — | N/A | TStreamable / stream I/O dropped project-wide (same as `Load`). |
| `Zoom` (method) | 581 | PORTED | OK | `tv::Window::zoom` (private method, called from `handle_event`) | 2 | C++ `zoom()`: calls `sizeLimits`, if `size != maxSize` saves `zoomRect = getBounds()` then `locate(0,0,max.x,max.y)`, else `locate(zoomRect)`. Rust matches exactly, plus pushes the `zoomed` flag to the frame child (needed for the zoom-vs-unzoom icon). The additional zoomed-flag push is a deliberate D3 push-down deviation, noted inline. The `zoom` method is private (not in public API), so no external doc entry; the `handle_event` doc comment mentions it — no independent score entry needed. |
| `cpBlueWindow` palette (8 entries: indices 1–8 as listed in `twindow.cpp` header comment) | 581 | EQUIVALENT | OK | `tv::WindowPalette::Blue` → `Role::FramePassive`, `Role::FrameActive`, `Role::FrameIcon`, `Role::ScrollBarPage`, `Role::ScrollBarControl`, `Role::ScrollerNormal`, `Role::ScrollerSelected`, reserved | 2 | Guide: 8-entry palette, indices 1–8: frame passive, frame active, frame icon, scrollbar page, scrollbar controls, scroller normal text, scroller selected text, reserved. Rust replaces the indexed palette with named `Role` entries selected by `WindowPalette`. Known idiomatic mapping: class Palette → `tv::Theme` (D7). The `WindowPalette` doc explains the Blue/Cyan/Gray families but not the individual 8-entry mapping — score 2. |
| `cpCyanWindow` palette (8 entries, indices 16–23) | 581 | EQUIVALENT | OK | `tv::WindowPalette::Cyan` → `Role::FrameCyan*` family | 2 | Same mapping as Blue but in the Cyan role family. See note on `cpBlueWindow`. |
| `cpGrayWindow` palette (8 entries, indices 24–31) | 581 | EQUIVALENT | OK | `tv::WindowPalette::Gray` → `Role::FrameGray*` family | 2 | Same mapping as Blue but in the Gray role family (used by dialogs). See note on `cpBlueWindow`. |
| `TTitleStr` type (`string[80]`) | 557 | NOT-PORTED | — | — | N/A | Pascal fixed-length string type used to type title fields. Rust uses `Option<String>` (heap-allocated, unbounded). No counterpart needed — the type was purely a Pascal/Borland constraint. |
| `TWindowInit` mixin (createFrame function pointer) | C++ only | NOT-PORTED | — | — | N/A | C++ multiple-inheritance mixin carrying the `createFrame` factory pointer. Not in the guide directly; surfaced by `initFrame` NOT-PORTED above. No Rust counterpart. |
| `wfMove / wfGrow / wfClose / wfZoom` flag constants | 578 | EQUIVALENT | OK | `tv::WindowFlags` fields `r#move`, `grow`, `close`, `zoom` | N/A | D5: flag word → struct-of-bools. Values match the C++ bit positions semantically; no separate constants exist. |
| `wpBlueWindow / wpCyanWindow / wpGrayWindow` palette constants | 578 | EQUIVALENT | OK | `tv::WindowPalette::Blue`, `Cyan`, `Gray` | N/A | Integer constants replaced by the `WindowPalette` enum — same three values, type-safe. |
| `wnNoNumber` constant | C++ only | EQUIVALENT | OK | `Window::new(…, 0)` → `View::number()` returns `None` | N/A | The sentinel `0` maps to `None` in `View::number()`; documented in the `number_to_option` helper comment. |

## Summary

- PORTED: 7   EQUIVALENT: 9   NOT-PORTED: 6   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 13   |   → concept: 0
- Notable finding: No correctness gaps or SUSPECT items. The most architecturally significant deviation is `cmSelectWindowNum` handling: C++ does it inline in `TWindow::handleEvent` (matching on `event.message.infoInt == number`), but Rust moves the whole mechanism to `program_handle_event` + `Desktop::select_window_num` + `Group::focus_by_number` — the window's `select_window_num` method is in the `#[delegate(skip(…))]` list and the `handle_event` doc comment explains why. This is correct and deliberate, but it is the one place where a reader looking for the C++ broadcast-handler will not find it in the Window at all.
