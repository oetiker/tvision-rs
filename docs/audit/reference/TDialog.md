# TDialog  (guide pp. 415–418)

Rust module(s): `src/dialog/dialog.rs`, `src/dialog/mod.rs`   |   magiblot: `include/tvision/dialogs.h` / `source/tvision/tdialog.cpp`

> TDialog has **no own fields** beyond those inherited from TWindow/TView. The
> guide documents 4 methods (Init, Load, HandleEvent, GetPalette, Valid), one
> `Palette` field (inherited from TWindow), and three full 32-entry dialog
> palettes (CGrayDialog, CBlueDialog, CCyanDialog with named semantic slots).

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `Init` (constructor) | 416 | PORTED | OK | `tv::Dialog::new(bounds: Rect, title: Option<String>) -> Dialog` | 2 | Guide: calls TWindow Init with `wnNoNumber`, sets `GrowMode=0`, `Flags=wfMove|wfClose`, `Palette=dpGrayDialog`. Rust `new` does all four: `Window::new(…, 0)` (no number), `set_flags(move|close)`, `set_grow_mode(default())`, `set_palette(WindowPalette::Gray)`. Exact match. Doc explains the four overrides but "how/when to prefer Dialog vs Window" is absent. |
| `Load` (stream constructor) | 416 | NOT-PORTED | — | — | N/A | DOS TStreamable/stream machinery is project-wide dropped (CLAUDE.md: "TStreamable → dropped (serde if revived)"). Version-sniffing logic (`ofVersion10` → `dpGrayDialog`) has no analog. |
| `HandleEvent` (method) | 416 | PORTED | OK | `tv::Dialog::handle_event` (impl `View::handle_event`) | 3 | Guide: calls TWindow.HandleEvent first; Esc generates `cmCancel` (posted as a command event); Enter broadcasts `cmDefault`; `cmOK`/`cmCancel`/`cmYes`/`cmNo` while modal call `endModal`. Rust: calls `self.window.handle_event(ev, ctx)` first; `Key::Esc` → `ctx.post(Command::CANCEL)` + clear; `Key::Enter` → `ctx.broadcast(Command::DEFAULT, None)` + clear; `Command::OK|CANCEL|YES|NO` while `state.modal` → `ctx.end_modal(c)` + clear. Modal `endModal` call → deferred `Deferred::EndModal` via `Context::end_modal` (D9, documented in module doc). All four cases faithful. `end_modal` deferred path documented explicitly in module doc and handle_event doc-comment. |
| `GetPalette` (method) | 416–417 | EQUIVALENT | OK | `tv::window::WindowPalette` (Gray/Cyan/Blue) + `tv::Dialog::set_palette` / `with_palette` | 2 | Guide: returns one of `CGrayDialog`/`CBlueDialog`/`CCyanDialog` based on the `Palette` field (`dpGrayDialog` default). Rust: `WindowPalette` enum (`Gray`/`Blue`/`Cyan`) replaces the C++ palette-index selector; `Dialog::new` sets `WindowPalette::Gray` by default; colour is resolved at draw time through `Theme` role families. Known idiomatic mapping: class Palette → `tv::Theme` (D7). Public accessors documented (what) but "how the three variants map to the 32-entry palette slots" is not surfaced in Rust docs. |
| `Valid` (method) | 417 | PORTED | OK | `tv::Dialog::valid` (impl `View::valid`) | 3 | Guide: returns True if command is `cmCancel`, otherwise calls `TGroup::Valid`. Rust: `if cmd == Command::CANCEL { true } else { self.window.valid(cmd, ctx) }` — exact translation. Delegates to the embedded window (which delegates to its group) for all non-Cancel commands, so a validator child can veto OK. Doc-comment explains both arms and why Cancel cannot be vetoed. |
| `Palette` field (inherited, 3 values) | 416 | EQUIVALENT | OK | `tv::window::WindowPalette` (Gray / Blue / Cyan) stored in `Window::palette` private field, exposed via `Dialog::set_palette` / `with_palette` | 2 | Guide: `Palette` (Word) field from TWindow, set to `dpGrayDialog` in TDialog.Init; consumer can assign `dpBlueDialog`/`dpCyanDialog`. Rust: private `Window::palette: WindowPalette` field; Dialog exposes `set_palette(WindowPalette)` and `with_palette(…)` builder. Default is `Gray`. Consumer API documented (what/when), but the three constants (`dpBlueDialog`, `dpCyanDialog`, `dpGrayDialog`) have no named Rust counterparts — callers pass enum variants directly. |
| `CGrayDialog` palette (32 entries, slots 1–32) | 417–418 | EQUIVALENT | OK | `tv::theme::Role::FrameGray*` + descendant widget `Role::*` entries in `tv::Theme` | 2 | Guide Table 19.39: 32 named slots — Frame Passive/Active/Icon (1–3), ScrollBar Page/Controls (4–5), StaticText (6), Label Normal/Selected/Shortcut (7–9), Button Normal/Default/Selected/Disabled/Shortcut/Shadow (10–15), Cluster Normal/Selected/Shortcut/Disabled (16–18/31), InputLine Normal/Selected/Arrows (19–21), History Arrow/Sides (22–23), HistoryWindow ScrollBar page/controls (24–25), ListViewer Normal/Focused/Selected/Divider (26–29), InfoPane (30), Reserved (32). Rust: each semantic group maps to its widget's `Role::*` entries under `WindowPalette::Gray`; `Theme::classic_blue` defines the RGB values. Known idiomatic mapping: class Palette → `tv::Theme`. The 32-slot-to-Role mapping is implicit in the Theme code; no single doc or table surfaces it for consumers. |
| `CBlueDialog` palette (32 entries) | 417–418 | EQUIVALENT | OK | `tv::theme::Role::Frame*` + widget `Role::*` entries under `WindowPalette::Blue` | 2 | Same semantic structure as CGrayDialog but in the Blue colour family. Rust maps identically to Blue roles in Theme. Doc gap: same as Gray — no cross-reference table. |
| `CCyanDialog` palette (32 entries) | 417–418 | EQUIVALENT | OK | `tv::theme::Role::FrameCyan*` + widget `Role::*` entries under `WindowPalette::Cyan` | 2 | Same semantic structure in the Cyan colour family. Rust maps to Cyan roles in Theme. |
| `streamableName` / `build` (TStreamable) | — | NOT-PORTED | — | — | N/A | TStreamable registration machinery (`name`, `build()`, stream operators `>>` / `<<`) is project-wide dropped (see CLAUDE.md). No Rust counterpart exists or is planned unless serde is revived. |

## Summary

- PORTED: 3   EQUIVALENT: 5   NOT-PORTED: 2   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 7   |   → concept: 0
- Most important finding: No gaps or suspect items — every guide entry is accounted for. The main doc shortfall is that the three-palette / 32-slot semantic mapping (Table 19.39) has no Rust-facing cross-reference: `WindowPalette::Gray/Blue/Cyan` are documented individually but no table or module doc surfaces which `Role::*` variant covers which of the 32 named dialog palette slots, which is the information a consumer most needs when theming a custom dialog.
