# Globals/procedures/constants  (guide pp. 331–346)

Rust module(s): `src/app/program.rs`, `src/app/application.rs`, `src/event/mod.rs`, `src/event/key.rs`, `src/dialog/filedlg.rs`, `src/dialog/msgbox.rs`, `src/widgets/editor.rs`, `src/widgets/history.rs`, `src/help.rs`, `src/view/view.rs`, `src/window/window.rs`   |   magiblot: `source/tvision/`, `include/tvision/`

> This section covers the globals, procedures, and constants of the Turbo Vision 2.0
> reference from `DoneDosMem` through `kbXXXX` (keyboard constants that begin on p.347).
> Entries are in guide order.

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `DoneDosMem` (procedure) | 331 | NOT-PORTED | — | — | — | DOS heap management for shell-out. Rust owns memory via allocator; `Application` comment notes no explicit setup/teardown needed (`application.rs` module doc). |
| `DoneEvents` (procedure) | 331 | EQUIVALENT | OK | `CrosstermBackend::drop` (`src/backend/crossterm_backend.rs:272`) | 2 | C++ disables mouse interrupt handler + hides mouse. Rust backend `Drop` calls `restore_terminal()` which teardowns crossterm raw mode and mouse. Idiomatic RAII. |
| `DoneHistory` (procedure) | 331 | EQUIVALENT | OK | `thread_local! HISTORY` auto-drops (`src/widgets/history.rs:68`) | 2 | C++ frees the history block. Rust uses a `thread_local! RefCell<Vec<HistRec>>` that drops automatically; no explicit free needed. Documented in module doc. |
| `DoneMemory` (procedure) | 331 | NOT-PORTED | — | — | — | C++ frees all cache buffers allocated via `GetBufMem`. No DOS heap/cache-buffer system in Rust port. |
| `DoneSysError` (procedure) | 332 | NOT-PORTED | — | — | — | C++ restores DOS interrupt vectors 09H, 1BH, 21H, 23H, 24H. DOS-era; no analog in crossterm/Rust. |
| `DoneVideo` (procedure) | 332 | EQUIVALENT | OK | `CrosstermBackend::drop` → `restore_terminal()` (`src/backend/crossterm_backend.rs:272`) | 2 | C++ restores initial screen mode, clears screen, restores cursor. Rust `CrosstermBackend::drop` calls `restore_terminal()`. Same RAII pattern as `DoneEvents` — both collapse into one `Drop` impl. |
| `DoubleDelay` (variable) | 332 | EQUIVALENT | OK | `MouseEventFlags::double_click: bool` on `MouseEvent` (`src/event/mod.rs`) | 1 | C++ is a global `Word = 8` (units: 1/18.2 s) controlling double-click detection. Rust delegates double-click detection to crossterm/OS; the result surfaces as `MouseEventFlags::double_click: bool` on the event. The timing tunable is gone; it was DOS-BIOS-tick specific. Score 1: the field is named but the heritage mapping and the missing tunable are undocumented. |
| `dpXXXX` constants (`dpBlueDialog=1`, `dpCyanDialog=2`, `dpGrayDialog=3`) | 332 | EQUIVALENT | OK | `tv::WindowPalette::{Blue, Cyan, Gray}` (`src/window/window.rs:56`) | 2 | C++ was a `Word` stored in `TDialog.Palette`. Rust is an enum on `Window`/`Dialog` via `set_palette(WindowPalette)`. Known idiomatic mapping: integer constant → enum variant. |
| `EditorDialog` (variable) | 333 | NOT-PORTED | — | — | — | C++ was a global procedural variable (`TEditorDialog = DefEditorDialog`) allowing app-wide override of editor dialog dispatch. Rust port handles editor dialogs directly via `Deferred::OpenMessageBox` and per-editor closure chains in `Program`; there is no single global function pointer. |
| `EditorFlags` (variable) | 333 | EQUIVALENT | OK | `Editor::editor_flags: u16` (per-instance field; `src/widgets/editor.rs:415`) | 1 | C++ was a global `Word = efBackupFiles + efPromptOnReplace`. Rust makes it a per-instance field on `Editor` rather than a global; set/get via `editor_flags()` / `set_editor_flags()`. The deviation from global → per-instance is undocumented in rustdoc. Score 1: field named, semantics not explained. |
| `edXXXX` constants | 333–334 | NOT-PORTED | — | — | — | C++ constants (`edOutOfMemory=0` .. `edReplacePrompt=10`) passed to the `EditorDialog` function to identify which dialog to show. No `EditorDialog` function-pointer mechanism was ported; editor dialogs are dispatched via `Deferred::OpenMessageBox` and `request_message_box`. |
| `efXXXX` constants | 334 | EQUIVALENT | OK | `EF_CASE_SENSITIVE`, `EF_WHOLE_WORDS_ONLY`, `EF_PROMPT_ON_REPLACE`, `EF_REPLACE_ALL`, `EF_DO_REPLACE`, `EF_BACKUP_FILES` (`src/widgets/editor.rs:87–98`, `pub(crate)`) | 1 | Six constants with matching values ($0001–$0100). All six are present. `pub(crate)` visibility — not public API. Score 1: named, no doc explaining semantics. |
| `EmsCurHandle` (variable) | 335 | NOT-PORTED | — | — | — | EMS (Expanded Memory Spec) state cache for `TEmsStream`. DOS EMS memory; no analog. |
| `EmsCurPage` (variable) | 335 | NOT-PORTED | — | — | — | EMS page state cache for `TEmsStream`. DOS EMS memory; no analog. |
| `ErrorAttr` (constant) | 335 | NOT-PORTED | — | — | — | C++ `const Byte = $CF`: error video attribute returned by `TView.GetColor` on palette mapping failure. Rust uses `Theme`/`Role` lookups; there is no raw video attribute fallback constant. Palette errors surface differently (no fallback color sentinel). |
| `evXXXX` constants — individual event types | 336 | EQUIVALENT | OK | `Event` enum variants: `Event::MouseDown`, `Event::MouseUp`, `Event::MouseMove`, `Event::MouseAuto`, `Event::KeyDown`, `Event::Command`, `Event::Broadcast`, `Event::Nothing` (`src/event/mod.rs:55–93`) | 3 | C++ `evMouseDown=$0001`, `evMouseUp=$0002`, `evMouseMove=$0004`, `evMouseAuto=$0008`, `evKeyDown=$0010`, `evCommand=$0100`, `evBroadcast=$0200`, `evNothing=$0000`. Rust: closed `Event` enum — no bitmask needed, each variant is structural. Known idiomatic mapping (D4). Module doc explains the heritage, each variant is documented. `evMouseWheel` is a Rust addition (`Event::MouseWheel`). |
| `evXXXX` mask constants — `evNothing`, `evMouse`, `evKeyboard`, `evMessage` | 336 | EQUIVALENT | OK | `EventMask::{mouse_move, mouse_auto}` + routing logic in `Group::handle_event` (`src/event/mod.rs:190`, `src/view/group.rs`) | 2 | C++ `evNothing=$0000`, `evMouse=$000F`, `evKeyboard=$0010`, `evMessage=$FF00` were used in `EventMask` field and routing predicates. Rust `EventMask` keeps only the two opt-in bits (`mouse_move`, `mouse_auto`); routing is structural (enum-match), not bitmask. Documented in `EventMask` heritage comment. Score 2: what the struct is, but the full routing logic lives in `Group` rather than being cross-linked. |
| `fdXXXX` constants | 337 | PORTED | OK | `FD_OK_BUTTON`, `FD_OPEN_BUTTON`, `FD_REPLACE_BUTTON`, `FD_CLEAR_BUTTON`, `FD_HELP_BUTTON`, `FD_NO_LOAD_DIR` (`src/dialog/filedlg.rs:1544–1554`) | 2 | All six constants, matching values ($0001–$0100). Public (`pub const`). Score 2: what each flag controls, but no "how/when to combine them" guidance. |
| `FindStr` (variable) | 337 | EQUIVALENT | OK | `Editor::find_str: String` (per-instance field; `src/widgets/editor.rs:411`) | 1 | C++ was a global `string[80]` holding the last search string across all editors. Rust makes it per-instance on `Editor`; coordinated across editors via `Program` broker logic (`program.rs:2589`). Deviation from global → per-instance is not documented in rustdoc. |
| `FNameStr` (type) | 337 | NOT-PORTED | — | — | — | C++ `FNameStr = string[79]`: a Pascal fixed-length string typedef for DOS file names. Rust uses `String`/`&str` throughout; no fixed-length file-name type. |
| `FocusedEvents` (variable) | 337–338 | EQUIVALENT | OK | Structural enum routing in `Group::handle_event` (`src/view/group.rs:1187–1202`) | 2 | C++ `Word = evKeyboard + evCommand`: global constant used by `TGroup.HandleEvent` to decide whether to route to focused child or broadcast. Rust uses enum-arm matching; "keyboard and command go to focused view, else broadcast" is enforced in `Group`'s match arms. The comment at `group.rs:1187` explains the routing. Score 2: the comment explains what, not how to configure it (no per-view override). |
| `FormatStr` (procedure) | 338–339 | NOT-PORTED | — | — | — | C++ `vsprintf`-style string formatter taking `Format: String; var Params`. Rust uses `format!` macro and standard `Display`/`write!` throughout; no `FormatStr` analog exists or is needed. |
| `FreeBufMem` (procedure) | 340 | NOT-PORTED | — | — | — | C++ frees a cache buffer via `DisposeCache`. DOS cache-buffer management; no analog in Rust port. |
| `GetAltChar` (function) | 340 | NOT-PORTED | — | — | — | C++ `function GetAltChar(KeyCode: Word): Char` — reverse-maps a 2-byte alt scan code to the character. Rust encodes Alt keys as `Key::Char(ch)` + `KeyModifiers::alt`; no scan-code reverse-lookup function needed. |
| `GetAltCode` (function) | 340 | NOT-PORTED | — | — | — | C++ `function GetAltCode(Ch: Char): Word` — maps a char to its alt 2-byte scan code. Same reason as `GetAltChar`; scan codes are not exposed in the Rust key model. |
| `GetBufMem` (procedure) | 340–341 | NOT-PORTED | — | — | — | C++ allocates a cache buffer. DOS cache-buffer management; no analog. |
| `GetBufferSize` (function) | 340 | NOT-PORTED | — | — | — | C++ returns size in bytes of a buffer allocated by `NewBuffer`. DOS memory management; no analog. |
| `GetKeyEvent` (procedure) | 341 | EQUIVALENT | OK | crossterm keyboard polling in `CrosstermBackend` / `Program::pump_once` (`src/app/program.rs:1641`) | 2 | C++ polls BIOS INT 16H. Rust polls via crossterm in the event loop; result surfaces as `Event::KeyDown`. The heritage is described in the module doc for events. Score 2: what the mechanism does, but the C++ heritage call is not cross-linked. CORRECTED (private-symbol re-check): was `src/app/program.rs:704` — line 704 is `pub fn run`; `pump_once` is at line 1641. |
| `GetMouseEvent` (procedure) | 341 | EQUIVALENT | OK | crossterm mouse polling in `CrosstermBackend` / `Program::pump_once` | 2 | C++ polls Turbo Vision's mouse queue. Rust polls via crossterm; results surface as `Event::MouseDown` etc. Same note as `GetKeyEvent`. |
| `gfXXXX` constants | 341–342 | EQUIVALENT | OK | `tv::GrowMode` struct-of-bools: `lo_x`, `lo_y`, `hi_x`, `hi_y`, `rel`, `fixed` (`src/view/view.rs:183–196`) | 2 | C++ `gfGrowLoX=$01`, `gfGrowLoY=$02`, `gfGrowHiX=$04`, `gfGrowHiY=$08`, `gfGrowRel=$10`, `gfGrowAll=$0F`. Rust: struct-of-bools (D5); `GrowMode::grow_all()` helper. Each field is documented with its `gf*` heritage name. Score 2: what each field does; "how/when to set combinations" could be stronger. |
| `hcXXXX` constants — `hcNoContext=0`, `hcDragging=1` (Views) | 343 | EQUIVALENT | OK | `HelpCtx::NO_CONTEXT`, `HelpCtx::DRAGGING` (`src/help.rs:54, 57`) | 3 | Direct counterparts; identity changed from `int` to `&'static str` (D1). Both are documented with purpose and when to use. Score 3. |
| `hcXXXX` constants — standard File menu help contexts (`hcNew`=`$FF01` .. `hcExit`=`$FF08`) | 342–343 | NOT-PORTED | — | — | — | C++ defined standard help context constants for `StdFileMenuItems`, `StdEditMenuItems`, `StdWindowMenuItems`. Rust has no `StdXxxMenuItems` helpers and no corresponding `hcXXXX` constants defined. App code uses `HelpCtx::custom("...")` to define its own. |
| `hcXXXX` constants — standard Edit menu help contexts (`hcUndo`=`$FF10` .. `hcClear`=`$FF14`) | 343 | NOT-PORTED | — | — | — | Same reason as File menu hcXXXX: no `StdEditMenuItems` function and no predefined edit-menu help context constants. |
| `hcXXXX` constants — standard Window menu help contexts (`hcTile`=`$FF20` .. `hcClose`=`$FF27`) | 343 | NOT-PORTED | — | — | — | Same reason: no `StdWindowMenuItems` and no predefined window-menu help context constants. |
| `HideMouse` (procedure) | 344 | EQUIVALENT | OK | crossterm mouse cursor hide in `CrosstermBackend` (internal) | 1 | C++ increments "hide counter" in mouse driver. Rust hides/shows mouse cursor via crossterm on terminal setup/teardown; no public `hide_mouse` API. Score 1: no public symbol. |
| `HiResScreen` (variable) | 344 | NOT-PORTED | — | — | — | C++ Boolean set by `InitVideo` if EGA/VGA 43/50-line mode available. No DOS video modes; Rust uses crossterm's terminal size query. Not ported. |
| `HistoryAdd` (procedure) | 344 | PORTED | OK | `tv::history_add(id: u8, str: &str)` (`src/widgets/history.rs:94`) | 2 | Direct counterpart. Signature change: `Id: Byte, Str: String` → `id: u8, str: &str` (idiomatic). Score 2: explains what it does; "when to call it relative to input-line commit" not in rustdoc. |
| `HistoryBlock` (variable) | 344 | EQUIVALENT | OK | `HISTORY: thread_local! RefCell<Vec<HistRec>>` (`src/widgets/history.rs:68`) | 2 | C++ `Pointer = nil`: a pointer to the raw history memory block. Rust: a `thread_local!` `Vec<HistRec>` that owns the data. The module doc documents the change. Score 2: module doc explains the structure; the direct mapping is not in a named public item. |
| `HistoryCount` (function) | 344–345 | PORTED | OK | `tv::history_count(id: u8) -> usize` (`src/widgets/history.rs:128`) | 2 | Direct counterpart; return type changed `Word` → `usize` (idiomatic). |
| `HistorySize` (variable) | 345 | EQUIVALENT | OK | `HISTORY_SIZE: usize = 1024` (`src/widgets/history.rs:72`, module-private `const`) | 1 | C++ `Word = 1024`: the byte budget for the history block. Rust is an internal constant with the same value. Not public; not documented beyond the inline comment. Score 1. CORRECTED (private-symbol re-check): was `pub(crate)` — actual declaration is `const HISTORY_SIZE` (module-private, not `pub(crate)`). |
| `HistoryStr` (function) | 345 | PORTED | OK | `tv::history_str(id: u8, index: usize) -> Option<String>` (`src/widgets/history.rs:135`) | 2 | Direct counterpart; return type changed `String` → `Option<String>` (idiomatic: returns `None` if out of range). |
| `HistoryUsed` (variable) | 345 | NOT-PORTED | — | — | — | C++ `Word = 0`: internal offset pointer within the raw history block. Rust uses a `Vec`; there is no raw-offset variable. Implementation detail dropped cleanly. |
| `InitDosMem` (procedure) | 345 | NOT-PORTED | — | — | — | C++ reclaims heap space after DOS shell by calling `SetMemTop`. No DOS heap; no analog. |
| `InitEvents` (procedure) | 346 | EQUIVALENT | OK | `CrosstermBackend::new` / terminal raw-mode setup (`src/backend/crossterm_backend.rs`) | 2 | C++ enables mouse interrupt handler and shows mouse. Rust `CrosstermBackend` construction sets up crossterm raw mode and mouse capture. Heritage documented in application.rs module doc. |
| `InitHistory` (procedure) | 346 | EQUIVALENT | OK | `thread_local! HISTORY` auto-initializes (`src/widgets/history.rs:68`) | 2 | C++ allocates the history block. Rust: `thread_local! RefCell::new(Vec::new())` — auto-initialized on first access. Documented in `application.rs` module doc comment ("History needs no explicit setup"). |
| `InitMemory` (procedure) | 346 | NOT-PORTED | — | — | — | C++ installs a heap notification function in `HeapError`. DOS heap management; no analog. |
| `InitSysError` (procedure) | 346 | NOT-PORTED | — | — | — | C++ captures DOS interrupt vectors. DOS-era; no analog. |
| `InitVideo` (procedure) | 347 | EQUIVALENT | OK | `CrosstermBackend::new` (terminal initialization path) | 2 | C++ saves `StartupMode`, switches screen mode, sets `ScreenWidth`/`ScreenHeight`/`HiResScreen`/`CheckSnow`/`ScreenBuffer`/`CursorLines`. Rust queries terminal size via `CrosstermBackend::size()` and uses crossterm for raw mode. |
| `InputBox` (function) | 347 | PORTED | OK | `Program::input_box(&mut self, title, label, s, limit) -> Command` (`src/app/program.rs:1066`) | 2 | Guide: 60×8 dialog, title, label, OK/Cancel, single input line. Rust auto-sizes and centers on desktop (`centered_msgbox_rect`). Signature close; `s: &str` and returns `Command` instead of modifying in-place. Score 2: what it does; sizing/centering deviation from 60×8 spec noted in rustdoc comment. |
| `InputBoxRect` (function) | 347 | PORTED | OK | `Program::input_box_rect(&mut self, r: Rect, title, label, s, limit) -> Command` (`src/app/program.rs:1021`) | 2 | Direct counterpart with explicit `Rect`. Score 2: what it does. |
| `kbXXXX` constants — keyboard state/shift masks (`kbRightShift`, `kbLeftShift`, `kbCtrlShift`, `kbAltShift`, `kbScrollState`, `kbNumState`, `kbCapsState`, `kbInsState`) | 347–348 | EQUIVALENT | OK | `KeyModifiers::{shift, ctrl, alt}` (`src/event/key.rs:76`); Insert/Caps/Scroll/Num Lock states not modeled | 2 | C++ was a packed `Word` with 8 shift-state bits. Rust collapses left/right shift, ctrl, alt into three bools; lock states (ScrollLock, NumLock, CapsLock, InsLock) are not modeled — crossterm does not expose them reliably cross-platform. Documented in heritage comment of `KeyModifiers`. Score 2: what the struct is; the deliberate drop of lock states could be more explicit. |
| `kbXXXX` constants — alt-letter scan codes (`kbAltA` .. `kbAltZ`) | 348 | EQUIVALENT | OK | `Key::Char(ch)` + `KeyModifiers { alt: true }` (`src/event/key.rs`) | 2 | C++ 2-byte scan codes. Rust: `Key::Char(lower_char)` + `alt = true`. Documented in key.rs heritage comment. |
| `kbXXXX` constants — special keys (`kbEqual`, `kbAltMinus`, `kbAltSpace`, `kbBack`, `kbCtrlBack`, `kbCtrlDel`, `kbCtrlEnd`, `kbCtrlEnter`, `kbCtrlHome`, `kbCtrlIns`, `kbCtrlLeft`, `kbCtrlPgDn`, `kbCtrlPgUp`, `kbCtrlPrtSc`, `kbCtrlRight`, `kbDel`, `kbDown`, `kbEnd`, `kbEnter`, `kbEsc`, `kbGrayMinus`, `kbGrayPlus`, `kbHome`, `kbIns`, `kbLeft`, `kbNoKey`, `kbPgDn`, `kbPgUp`, `kbRight`, `kbShiftDel`, `kbShiftIns`, `kbShiftTab`, `kbTab`, `kbUp`) | 348–349 | EQUIVALENT | OK | `Key` enum variants + `KeyModifiers` flags (`src/event/key.rs:28–64`) | 2 | C++ scan codes. Rust: structural `Key` enum (Enter, Esc, Backspace, Tab, Up, Down, Left, Right, Home, End, PageUp, PageDown, Insert, Delete) + `KeyModifiers`. `kbNoKey` → absence of `Event::KeyDown`. `kbGrayMinus`/`kbGrayPlus` are keypad variants — `Key::Char('-'/'+'`) on most crossterm mappings. Heritage documented in module doc. |
| `kbXXXX` constants — alt-number scan codes (`kbAlt1`..`kbAlt0`) | 349 | EQUIVALENT | OK | `Key::Char('1'..'0')` + `KeyModifiers { alt: true }` | 2 | Same encoding as alt-letters. |
| `kbXXXX` constants — function keys (`kbF1`..`kbF10`) | 349 | EQUIVALENT | OK | `Key::F(1)` .. `Key::F(10)` | 2 | Direct counterpart. |
| `kbXXXX` constants — shift-function keys (`kbShiftF1`..`kbShiftF10`) | 349 | EQUIVALENT | OK | `Key::F(n)` + `KeyModifiers { shift: true }` | 2 | |
| `kbXXXX` constants — ctrl-function keys (`kbCtrlF1`..`kbCtrlF10`) | 349 | EQUIVALENT | OK | `Key::F(n)` + `KeyModifiers { ctrl: true }` | 2 | |
| `kbXXXX` constants — alt-function keys (`kbAltF1`..`kbAltF10`) | 350 | EQUIVALENT | OK | `Key::F(n)` + `KeyModifiers { alt: true }` | 2 | |

## Summary

- PORTED: 6   EQUIVALENT: 28   NOT-PORTED: 22   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 33   |   → concept: 0
- Notable finding: The Init*/Done* DOS subsystem lifecycle procedures (InitDosMem, InitSysError, InitEvents, InitMemory, InitVideo, DoneDosMem, DoneSysError, DoneMemory) are the single largest NOT-PORTED group (8 entries); they collapse cleanly into the `CrosstermBackend` RAII `Drop` and auto-initializing `thread_local!` store — a deliberate, architecture-level deviation that is documented in `application.rs`'s module doc but is not surfaced in individual rustdoc items. The 22 NOT-PORTED entries are all either DOS/EMS memory management, DOS interrupt vectors, or scan-code utilities that have no crossterm/Rust equivalent.
